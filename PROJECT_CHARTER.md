# Super Fitness — AI Smart Coach — ملف المشروع الرئيسي

> **هذا الملف هو المرجع الوحيد لكل من يعمل على هذا الفيتشر — إنت، وأي Coding Agent.**
> أي قرار تنفيذي لازم يرجع للمستند التقني الأصلي: `Fitness_Chatbot_Ollama_Cloud_Integration_v1_1.pdf`
> (الإصدار 1.1 — 25 يوليو 2026 — الموديل `gemma4:31b` عبر Ollama Cloud)

---

## 0. القرارات الأساسية (Decisions Log)

| القرار | الاختيار | السبب |
|---|---|---|
| لغة الـ Gateway | **Python 3.11+ / FastAPI** | مطابق تمامًا لكل الأمثلة البرمجية في المستند (async, httpx, sqlite3) — صفر ترجمة، صفر مخاطرة |
| الموديل | `gemma4:31b` عبر Ollama Cloud (`https://ollama.com/api/chat`) | كما في المستند — مباشر (Direct HTTPS)، بدون proxy محلي |
| الاستضافة | VPS في **Frankfurt (Hetzner Cloud)** أو **Fly.io (fra region)** | حل وسط بين قرب اليوزر (مصر/الشرق الأوسط) وقرب Ollama Cloud (أمريكا/أوروبا) — راجع §7.4 في المستند. غيّرها لاحقًا بقياس `ttft_ms` حقيقي، مش بالحدس |
| نقطة البداية | من الصفر بالكامل (Phase 0a) | الباك اند الحالي بتاع التطبيق (بيانات التمارين/الوجبات) منفصل تمامًا عن هذا الـ Gateway الجديد |
| خطة التوسع | Free tier أولاً (`OLLAMA_MAX_INFLIGHT=1`) → ترقية لاحقًا بدون إعادة كتابة كود | راجع §1.6 و §4.6 |
| مصدر الداتا | `exercises.db` و `meals.db` جاهزين بالفعل (مُصدَّرين من الباك اند الحالي) | **Phase 0b (بناء القاعدة من JSON) غير مطلوبة** — الملفات جاهزة بالفعل بنفس البنية المطلوبة في الملحق A من المستند |
| التحقق من هوية اليوزر (Auth) | الـ Gateway بيتحقق من التوكن عن طريق GET لـ `https://fitness.elevateegy.com/api/v1/auth/profile-data` بنفس التوكن — لو 200 يبقى صحيح، لو 401/403 يترفض | endpoint مخصص لبيانات بروفايل اليوزر — أنسب وأخف من استخدام endpoint بيانات تمارين لمجرد فحص التوكن |

---

## 0.5 مين بيعمل إيه (تقسيم التنفيذ)

| الجزء | مين بينفذه | إيه دوره |
|---|---|---|
| **الـ Gateway (السيرفر بالكامل)** | **Open Code** | ينفذ الكود بالكامل من البرومت للنهاية. عبدالملك مش متخصص backend، فالبرومتات دي بتتدّي لـ Open Code زي ما هي، وهو اللي بيكتب ويشغّل ويختبر |
| **تطبيق الفلاتر (كل حاجة UI/State/التكامل مع الـ Gateway)** | **عبدالملك نفسه، في Android Studio، بمساعدة Gemini** | عبدالملك بيوجّه الموديل ويكتب معاه، وفاهم كل سطر بيتكتب — ده تخصصه الأساسي |

**علشان كده كل مرحلة تحت متعلّم عليها مين ينفذها.** لو مرحلة "Open Code"، انسخ البرومت زي ما هو وادّيه له كامل. لو مرحلة "Android Studio / Gemini"، دي شغلانتك إنت المباشرة، والبرومت هنا مرجع ليك انت تفهم منه المطلوب وتوجّه بيه Gemini وإنت شايف كل حاجة بتتكتب.

---

## 1. القواعد الصارمة اللي ممنوع تتكسر أبدًا (Non-Negotiable Rules)

هذه القواعد مستخرجة حرفيًا من المستند. أي Coding Agent يشتغل على المشروع لازم يلتزم بيها 100%، وأي كود يخالفها **يُرفض فورًا** بغض النظر عن أي سبب (سرعة، تجربة، "مؤقتًا"):

1. **الموديل ممنوع يشوف الداتا بيز أبدًا.** هو بس بيبعت tool calls، والـ Gateway هو اللي بيحول الطلب لـ SQL. (§1.1, §3.1)
2. **الموديل ممنوع يخترع أي id.** أي `exercise_refs` / `meal_refs` راجعة من الموديل لازم تتفحص (`validate_refs`) ضد الـ candidate set بتاع نفس الـ turn، مش ضد الداتا بيز كلها. (§3.9)
3. **الـ API Key بتاع Ollama ممنوع يوصل للتطبيق (Flutter) نهائيًا.** مكانه الوحيد: secret manager على السيرفر. (§1.2, §4.8)
4. **الموديل ممنوع يطلع URL أو رابط فيديو في الرد النهائي.** الروابط (`demo_url`, `explain_url`) بيحطها التطبيق بنفسه وقت العرض من الداتا بيز المحلية. (§2.3, §3.3 rule 3)
5. **`"think": false` إجباري على الـ turn A والـ turn B الاتنين.** بدون استثناء، وبدون الاعتماد على القيمة الافتراضية. (§3.12)
6. **ممنوع إرسال `num_ctx` أو `keep_alive`** — مش مدعومين على Cloud models، وإرسالهم بيخلي اللوجز تكدب. (§1.6, §3.12)
7. **كل استعلام SQL لازم يكون parameterised.** ممنوع نهائيًا عمل string formatting لأي argument راجع من الموديل جوه الـ SQL — الموديل مصدر غير موثوق (untrusted input). (§3.5)
8. **الـ retrieval لازم يمر بـ "سلم التخفيف" (relaxation ladder) قبل ما يرجع نتيجة فاضية.** صفر نتائج = تجربة سيئة يجب تجنبها دايمًا. (§3.6)
9. **وضع الطوارئ (Degraded Mode) إجباري وليس اختياريًا.** لو الموديل مش متاح (quota خلصت، 5xx، مفتاح اتلغى)، الـ retrieval السادة لازم يفضل شغال ويرجع كروت — التطبيق ممنوع يعرض شاشة خطأ فاضية. (§4.11)
10. **الماكروز الغذائية دايمًا تقريبية.** أي رقم غذائي لازم يتقال بصيغة "تقريبًا/حوالي" مع علامة `estimated`. ممنوع عرضه كرقم نهائي. (§2.4, قائمة المخاطر §7.7)
11. **مسائل السلامة (إصابة، حمل، اضطرابات أكل، أدوية) لازم يترفض فيها أي توصية تمرين/وجبة**، ويترفع `safety_flag`، ويتقال للمستخدم يراجع مختص. الموديل مش دكتور. (§3.3 rule 6)
12. **الاختبارات (eval set) لازم تتعمل بالعربي والإنجليزي معًا، مش ترجمة واحدة للتانية.** الجودة بالعربي نقطة ضعف معروفة في الموديلات ثنائية اللغة. (§7.5, §7.7)

---

## 2. خريطة المكونات الثلاثة

| المكوّن | بيمتلك | ممنوع يعمل |
|---|---|---|
| **Flutter App** | العرض، التنقل (`go_router`)، نسخة أوفلاين من الداتا بيز | يكلم Ollama مباشرة، يشيل الـ API key، يبني الـ prompt |
| **Gateway (السيرفر الجديد)** | الـ system prompt، تعريفات الـ tools، الـ retrieval SQL، التحقق من الـ refs، المفتاح، الـ rate limiting، الـ quota | يرسل للتطبيق أي حاجة ماتحققش منها |
| **Ollama Cloud** | الفهم اللغوي والتوليد بس | يشوف الداتا بيز، أو الشبكة، أو هوية اليوزر |

---

## 3. خطة التنفيذ بالمراحل (مبنية على §7.8 في المستند)

> **Phase 0 و 1 و 2 ممكن تتوازى.** Phase 0a لازم تتعمل قبل أي تقدير وقت لباقي المراحل — نص يوم شغل بيأكد كل افتراضات §3 و §4.

### Phase 0a — اختبار القدرة (نص يوم) — 🔧 ينفذها **Open Code**
**الهدف:** التأكد إن `gemma4:31b` فعلاً بيرجع tool_calls و structured JSON على حسابك تحديدًا.
**تم لما:** ظهرت `tool_calls` صحيحة و JSON مطابق للـ schema من استدعاء حقيقي لـ `ollama.com`.

**البرومت للـ Agent:**
```
اعمل سكريبت Python بسيط (~20 سطر) اسمه scripts/phase0_capability_check.py
يعمل الآتي بالترتيب، ويطبع نتيجة كل خطوة بوضوح (PASS/FAIL):

1. يقرأ OLLAMA_API_KEY من متغير بيئة، ويتأكد إنه موجود ومش فاضي.
2. يعمل GET لـ https://ollama.com/api/tags بـ Authorization: Bearer $OLLAMA_API_KEY
   ويتأكد إن gemma4:31b موجود في قائمة الموديلات.
3. يعمل POST لـ https://ollama.com/api/chat بموديل gemma4:31b و tools=[search_exercises]
   (استخدم تعريف الأداة بالظبط من قسم 3.4 في المستند) و think=false و stream=false،
   برسالة "beginner glute exercises, mat only"،
   ويتأكد إن الرد فيه tool_calls[0].function.name == "search_exercises".
4. يعمل POST تاني بنفس الموديل لكن بـ format=<الـ JSON Schema بالظبط من قسم 3.8>
   و think=false، ويتأكد إن الرد بعد فك الـ JSON فيه مفتاح "reply".

اطبع في الآخر ملخص واضح: أي خطوة نجحت وأي خطوة فشلت، ولو فشلت خطوة وضّح الرسالة كاملة.
لا تكمل لأي حاجة تانية في المشروع قبل ما ترجعلي نتيجة هذا السكريبت.
```

**تحذير:** لو الخطوة التانية فشلت (الموديل مش متاح على حسابك)، **استوقف كل حاجة** وارجعلي فورًا — وقتها الخطة بتتغير، لأن ده معناه محتاجين خطة مدفوعة قبل ما نبدأ التطوير مش بعده.

---

### Phase 0b — ⛔️ ملغاة (غير مطلوبة)
**السبب:** عبدالملك مديله نسخة جاهزة من `exercises.db` و `meals.db` مباشرة من الباك اند الحالي، فمفيش داعي لتشغيل `build_catalog_db.py` من الصفر. بدل كده، أول ما نوصل Phase 3 (الاسترجاع الحقيقي)، أول حاجة نتأكد منها هي إن شكل الجداول والـ views (`exercise_card`, `meal_card`, إلخ) في الملفين دول مطابق فعليًا لبنية الملحق A في المستند — لو فيه أي اختلاف في أسماء الأعمدة، هنعدّل الاستعلامات على أساسه وقتها.

---

### Phase 1 — الأساس المحلي (Flutter، بدون chatbot) — 📱 ينفذها **عبدالملك في Android Studio بمساعدة Gemini**
**الهدف:** `AssetInstaller` + `CatalogDao` + شاشات تصفح/بحث بتشتغل من غير أي اتصال بالإنترنت.
**تم لما:** الكتالوج شغال 100% أوفلاين، تصفح وبحث بالعربي والإنجليزي.

**البرومت للـ Agent:**
```
نفّذ في مشروع Flutter الحالي (Super Fitness):
1. AssetInstaller يثبت exercises.db و meals.db من assets لمجلد قواعد البيانات،
   بالتحقق من sha256 من manifest.json، وبآلية temp-file-then-rename
   عشان لو التطبيق اتقفل نص النسخ ميفضلش فيه ملف تالف.
2. CatalogDao (فتح القاعدتين بـ readOnly: true) بدوال:
   exercisesByIds, exerciseById, browse (بفلاتر muscle_group/equipment/max_difficulty)،
   search (بحث FTS5 ثنائي اللغة مع escape للمدخلات قبل MATCH).
3. شاشتين: قائمة تمارين قابلة للفلترة، وتفاصيل تمرين واحد.

اتبع كود المستند في قسم 5.1 إلى 5.4 حرفيًا، وارجعلي لما يشتغل التصفح والبحث
بدون أي اتصال إنترنت، وابعتلي لقطة شاشة أو وصف لتجربة التشغيل.
```

---

### Phase 2 — هيكل الـ Gateway (بدون ذكاء حقيقي بعد) — 🔧 ينفذها **Open Code**
**الهدف:** مصادقة، rate limiting، عميل Ollama بالمفتاح وبوابة التزامن، SSE، تسجيل (logging).
**تم لما:** محادثة بسيطة بتتدفق (stream) بنجاح، و `ttft_ms` و `queue_ms` بيتقاسوا فعليًا.

**البرومت للـ Agent:**
```
ابنِ مشروع FastAPI جديد اسمه gateway/ يحتوي على:

1. الوحدة upstream_client.py: بالظبط زي كود قسم 4.6 في المستند —
   MAX_INFLIGHT من env، QUEUE_DEPTH من env، الأصناف Busy/QuotaExhausted/
   UpstreamDown/BadCredentials/UpstreamRejected، ودالة raise_for_upstream،
   ودالة post_json بإعادة محاولة على 5xx/اتصال فقط (أبدًا على 401/403/429).

2. Endpoint POST /v1/chat: يتحقق أولاً من هوية اليوزر — التطبيق هيبعت
   التوكن بتاعه (نفس التوكن المخزّن في flutter_secure_storage بعد
   signin/signup) في الـ header بصيغة "Authorization: Bearer <token>".
   الـ Gateway يتحقق منه بعمل GET لـ
   https://fitness.elevateegy.com/api/v1/auth/profile-data
   بنفس الـ Authorization header اللي جاله من التطبيق:
   - لو الرد 200: التوكن صحيح، كمّل.
   - لو الرد 401/403: ارجع 401 للتطبيق فورًا، من غير ما تكلم Ollama خالص.
   - لو أي خطأ تاني (طول الاتصال، 5xx): عاملها كخطأ Auth غير متاح،
     وارجع 401 بحذر (لا تفترض إن التوكن صحيح لمجرد إن endpoint التحقق فشل).
   اكتب الاستدعاء ده في دالة منفصلة verify_user_token(token) قابلة
   للاستبدال بسهولة لو غيّرنا الـ endpoint لاحقًا. لا تبني نظام تحقق JWT
   محلي بسر — التحقق دايمًا عن طريق الباك اند الخارجي.
   بعد التحقق، رد بـ SSE أربعة
   أحداث فقط: token, refs, done, error — بنفس الصيغة في قسم 4.3.
   في هذه المرحلة، رد بمحتوى وهمي ثابت (placeholder) في الـ reply —
   الذكاء الحقيقي هييجي في Phase 3 و 4.

3. GET /healthz: يفحص قاعدة البيانات المحلية، والوصول لـ ollama.com،
   وصلاحية المفتاح، ووجود الموديل — بالظبط زي كود قسم 4.2، مع الـ cache
   لمدة 60 ثانية على فحص الـ upstream.

4. Rate limiting: حد أقصى محافظ لكل يوزر (ابدأ بـ 6 رسائل/دقيقة طالما
   MAX_INFLIGHT=1، زي ما موضح في قسم 4.8).

5. Logging: سطر JSON واحد لكل turn، بنفس الحقول في قسم 4.9
   (حتى لو بعضها فاضي في هذه المرحلة).

لا تضف أي منطق retrieval أو system prompt حقيقي الآن — فقط الهيكل.
اختبر الـ endpoint محليًا وابعتلي نتيجة /healthz.
```

---

### Phase 3 — الاسترجاع الحقيقي (Retrieval) + المسار السريع — 🔧 ينفذها **Open Code**
**الهدف:** الموديل بيرشّح تمارين ووجبات حقيقية من القاعدة، والـ fast path بيشتغل.
**تم لما:** توصيات حقيقية تظهر، ونسبة استخدام الـ fast path متقاسة.

**البرومت للـ Agent:**
```
أضف في الـ gateway:

1. VOCAB_SQL وbuild_vocabulary_block() بالظبط زي قسم 3.2 — تُبنى مرة واحدة
   عند إقلاع السيرفر، ومُخزّنة في الذاكرة، وتُعاد بناؤها فقط عند تغيّر data_version.

2. system prompt كملف نصي منفصل ومُصدّر (versioned) — انسخ محتواه بالظبط
   من قسم 3.3، مع استبدال {APP_NAME}, {locale}, {level}, {goal}, {equipment},
   {limits}, {age_band}, {units}, {VOCABULARY_BLOCK}.

3. تعريفات الأدوات الثلاث (search_exercises, search_meals, search_by_text)
   بالظبط زي قسم 3.4.

4. استعلامات SQL بارامترية لكل أداة (قسم 3.5 و B.1/B.2 في الملحق)،
   مع سلّم التخفيف (relaxation ladder) بنفس ترتيب قسم 3.6 —
   FILTER_ORDER = ["movement_pattern", "mechanics", "body_region",
   "max_difficulty", "equipment", "muscle_group"].

5. تسلسل البطاقات (card serialization) بصيغة pipe-delimited بالظبط
   زي قسم 3.7 — ممنوع JSON هنا.

6. حلقة الـ two-turn (قسم 4.5) كاملة: turn A بدون stream، ثم استدعاء
   الأدوات، ثم turn B بـ stream=true و format=OUTPUT_SCHEMA (قسم 3.8).

7. المسار السريع (fast path، قسم 4.7): مصنّف بسيط بـ regex يستخرج
   muscle_group/equipment/max_difficulty من نص الرسالة مباشرة إلى قيم
   الـ vocabulary (مش الكلمات الخام)، ويتخطى turn A بالكامل لو نجح.
   سجّل نسبة استخدامه في اللوج.

اختبر بالأسئلة دي بالظبط وابعتلي الرد الكامل لكل سؤال:
- "beginner glute exercises, I only have a mat"
- "تمارين بطن للمبتدئين"
- "kettlebell exercises" (المفروض يرجع نتيجة بعد تخفيف الفلاتر، مش فاضية)
```

---

### Phase 4 — التحقق + وضع الطوارئ (Degraded Mode) — 🔧 ينفذها **Open Code**
**الهدف:** كل id راجع من الموديل اتحقق منه، ووضع الطوارئ شغال فعليًا.
**تم لما:** حذف المفتاح تجريبيًا لسه بيرجع كروت حقيقية للمستخدم.

**البرومت للـ Agent:**
```
أضف في الـ gateway:

1. validate_refs() بالظبط زي قسم 3.9 — التحقق من الـ id مرتين:
   أولاً ضد candidate_ids بتاعة هذا الـ turn تحديدًا، وثانيًا ضد الكتالوج
   نفسه (احتمال id اتشال). سجّل كل حالة drop مع رد الموديل الخام.

2. handle_chat() كطبقة خارجية وحيدة تستدعي chat()، وكل الأخطاء المُصنّفة
   (Busy, QuotaExhausted, BadCredentials, UpstreamDown) تتحول لـ
   chat_degraded() — بالظبط زي قسم 4.5 و 4.11. تأكد إن لا يوجد أي مكان
   في الكود يستدعي chat() مباشرة.

3. chat_degraded(): يستخدم مصنّف الـ fast path نفسه لاستخراج الفلاتر،
   يشغّل retrieval محلي بدون أي اتصال بـ Ollama، يرسل سطر جاهز (canned)
   بالعربي والإنجليزي زي قسم 4.11، ويضبط degraded: true في حدث done.

4. جدول الأخطاء الكامل في قسم 4.10 — نفّذه سطر بسطر: أعد المحاولة مرة
   واحدة فقط على 5xx/timeout في turn A، وفي turn B فقط لو chars == 0،
   وأبدًا على 401/403/429.

اختبر بحذف/تخريب OLLAMA_API_KEY مؤقتًا في بيئة التطوير، وأرسل رسالة،
وتأكد إنك استلمت كروت حقيقية + رسالة "الكوتش مشغول دلوقتي" بدل أي شاشة
خطأ فاضية. ابعتلي لقطة من استجابة الـ SSE كاملة في الحالتين (عادي وطوارئ).
```

---

### Phase 5 — واجهة الشات في Flutter — 📱 ينفذها **عبدالملك في Android Studio بمساعدة Gemini**
**الهدف:** تجربة كاملة من طرف لطرف — كتابة، بث الرد، كروت، تنقل.
**تم لما:** end-to-end شغال بالكامل.

**البرومت للـ Agent:**
```
في تطبيق Flutter، أضف:

1. ChatApi (قسم 5.5): يقرأ SSE stream من /v1/chat ويحوّله لأحداث
   TokenEvent/RefsEvent/DoneEvent/ErrorEvent.

2. ChatController (قسم 5.6): يبني على الترتيب الصحيح — الكروت (refs)
   تظهر بعد آخر token دايمًا، مش قبله. الترطيب (hydration) من القاعدة
   المحلية أولاً، وسناب شوت الـ gateway بس كـ fallback.

3. AssistantBubble (قسم 5.7): نص + شريط أفقي من الكروت، كل كارت بيفتح
   شاشة التمرين/الوجبة الموجودة بالفعل في التطبيق (نفس route table،
   قسم 6.1) — ممنوع شاشة تفاصيل جديدة خاصة بالشات.

4. أزرار الـ action (قسم 6.2): افتح تمرين/وجبة، افتح قائمة مفلترة،
   ابدأ تمرين — بس لو الـ payload فيه id موجود فعليًا محليًا.

5. التصميم المرئي لحالة degraded (قسم 5.8): نص أهدأ، بدون أيقونة خطأ،
   بدون زر إعادة محاولة لو الكود quota. الكروت تُعرض عادي تحتها.

6. حفظ نص الرسالة في خانة الكتابة عند أي خطأ (ممنوع مسحها).

اختبر المحادثة الكاملة بالعربي والإنجليزي، وابعتلي فيديو قصير أو
تسلسل لقطات شاشة للتجربة.
```

---

### Phase 6 — التقييم، السلامة، القياس، الإطلاق — 🔧🤝 مشتركة: البنية التحتية والمصنّفات مع **Open Code**، ومراجعة القائمة النهائية إنت وأنا
**الهدف:** جاهز للإطلاق فعليًا.
**تم لما:** كل عنصر من قائمة الإطلاق في القسم 8 تحت اتعمل.

**البرومت للـ Agent:**
```
جهّز:

1. مجموعة اختبار من 30 سؤال (15 عربي، 15 إنجليزي) تغطي كل الفئات
   في قسم 7.5 (بسيط، مقيّد، مستحيل، تقني، غذائي، سلامة، خارج الموضوع،
   غامض، وحالة الطوارئ). اكتب سكريبت يشغّلها ويتحقق آليًا:
   كل ref موجود فعلاً، كل ref ضمن candidate set، safety_flag مضبوط
   صح، لا يوجد أي URL في reply، لغة الرد تطابق لغة السؤال.

2. مصنّف سلامة بسيط على الـ gateway نفسه (مش استدعاء موديل تاني) —
   يشتغل حتى في وضع الطوارئ — يرصد كلمات إصابة/حمل/اضطراب أكل/دواء
   ويرجع safety_flag المناسب بدون أي exercise_refs.

3. لوحة مراقبة بسيطة (حتى لو مجرد استعلامات على اللوج) لمؤشرات
   قسم 7.6: refs_dropped rate, empty-candidate rate, p95 ttft,
   upstream 429 rate, degraded-turn rate.

4. راجع معي قائمة الإطلاق الكاملة في القسم 8 من هذا الملف بندًا بندًا.
```

---

## 4. توصيات البنية التحتية (Infrastructure)

- **الاستضافة:** Hetzner Cloud — سيرفر صغير (2 vCPU / 4GB كافي جدًا؛ الخدمة عديمة الحالة تقريبًا بخلاف ملفي SQLite للقراءة فقط) في منطقة **Frankfurt (fsn1)**. البديل: Fly.io بمنطقة `fra`.
- **متغيرات البيئة المطلوبة (قسم 7.3):**
  ```
  OLLAMA_HOST=https://ollama.com
  OLLAMA_MODEL=gemma4:31b
  OLLAMA_API_KEY=<من secret manager، أبدًا في الكود>
  OLLAMA_MAX_INFLIGHT=1
  OLLAMA_QUEUE_DEPTH=8
  ```
- **مفتاح منفصل لكل بيئة** (staging مختلف عن production) — إلغاء مفتاح مسرّب ميوقفش الإنتاج.
- **فحص وقت الـ deploy (قسم 7.3):** ثلاث فحوصات إجبارية قبل أي إصدار جديد يستقبل traffic — الموديل مُدرج، الأدوات (tools) بتشتغل فعلاً، الـ structured output برجع JSON صحيح.

---

## 5. سجل تنفيذ المشروع (Execution History Log)

> **قاعدة:** كل مرة يخلص فيها الـ Agent مرحلة أو خطوة مهمة، يُضاف سطر هنا بالتاريخ.
> لا تحذف أي سطر قديم — هذا السجل تراكمي من أول يوم لحد الإطلاق.

| التاريخ | المرحلة | اللي اتعمل | الحالة | ملاحظات |
|---|---|---|---|---|
| 2026-07-27 | Phase 0a | إنشاء وتشغيل `scripts/phase0_capability_check.py` — التحقق من المفتاح، وجود الموديل، tool calls، structured output | ✅ تم | 4/4 نجاح. اكتشاف مهم: `format:<full schema>` غير مدعوم من `gemma4:31b` على Ollama Cloud — الحل استخدام `format:"json"` + system prompt |
| 2026-07-27 | Phase 2 | هيكل الـ Gateway: `gateway/upstream_client.py` (من §4.6 بالظبط)، `gateway/main.py` (FastAPI, `/healthz`, `/v1/chat`, auth, rate limit, logging) | ✅ تم | `/healthz` يفحص 3 حاجات (الداتابيز، ollama، الموديل) مع cache 60ث. `/v1/chat` بيتحقق من التوكن عبر `fitness.elevateegy.com/api/v1/auth/profile-data` ويرد SSE بـ 4 أحداث (token, refs, done, error). Rate limit 6/دقيقة. الاختبارات: `/healthz` → 200, chat بدون auth → 401, chat بتوكن وهمي → 401 |

---

## 6. قائمة ما قبل الإطلاق (مختصرة من الملحق C.6)

### بيانات
- [ ] `validation_report.json` بدون أخطاء، والـ build شغال بـ `--strict` في CI
- [ ] الماكروز الغذائية مليانة من مصدر حقيقي (USDA FoodData Central) مش تقديرات فاضية

### Ollama Cloud
- [ ] المفتاح في secret manager، ومفتاح منفصل لكل بيئة
- [ ] فحص الـ deploy الثلاثي (§7.3) شغال في الـ pipeline
- [ ] `think: false` متأكد منه في طلب حقيقي مُلتقط، مش بس في الكود
- [ ] `OLLAMA_MAX_INFLIGHT` مطابق للخطة المدفوعة فعليًا ÷ عدد الـ replicas

### Gateway
- [ ] لا يوجد أي مسار كود يستدعي `chat()` مباشرة — كله عبر `handle_chat()`
- [ ] وضع الطوارئ مُختبر فعليًا بإلغاء المفتاح في staging
- [ ] تنبيهات على `refs_dropped`، معدل 429، معدل degraded

### التطبيق
- [ ] الكروت السناب شوت تُعرض لكن لا تنتقل (non-tappable)
- [ ] الشات معطّل أوفلاين برسالة واضحة، والكتالوج شغال بالكامل أوفلاين
- [ ] نص الكتابة يُحفظ عند أي خطأ

### الجودة والسياسات
- [ ] مجموعة الـ 30 سؤال (نص عربي) تُشغَّل أسبوعيًا وقبل كل تعديل في الـ prompt
- [ ] لا يوجد أي URL في `reply` أبدًا
- [ ] سياسة الخصوصية تفصح عن معالجة طرف ثالث (Ollama) والمناطق (أمريكا/أوروبا/سنغافورة)

---

*آخر تحديث لهذا الملف: يبدأ من تاريخ إنشائه. حدّثه أول بأول مع تقدم التنفيذ.*
